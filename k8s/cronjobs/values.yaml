image:
  repository: ""
  tag: ""
  pullPolicy: IfNotPresent

job:
  name: biosamples-curation
  command: ["java", "-jar", "/app/app.jar"]
  args: []
  resources:
    limits:
      cpu: "500m"
      memory: "1Gi"
    requests:
      cpu: "200m"
      memory: "512Mi"

workflow:
  ttlStrategy:
    secondsAfterSuccess: 3600
    secondsAfterFailure: 7200
  retryStrategy:
    limit: 1
    backoff:
      duration: "10s"
      factor: 1
      maxDuration: "1m"

imagePullSecrets:
  - name: __set-in-commandline__
