image: docker:latest

stages:
  - build


maven-build:
  image: maven:3.6-jdk-11
  stage: build
  script:
    - mvn -X --version
    - cp ci_settings.xml /root/.m2/settings.xml
    - cat /root/.m2/settings.xml
    - mvn -X -T 2C -P embl-ebi clean package -Dembedmongo.wait --quiet
