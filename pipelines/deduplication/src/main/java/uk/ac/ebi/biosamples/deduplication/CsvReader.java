package uk.ac.ebi.biosamples.deduplication;

/*
Utility class to read CSV
public class CsvReader {
    public static List<String> readCsv() throws IOException {
        List<String> records = new ArrayList<>();
        try (BufferedReader br = new BufferedReader(new FileReader("anyfile"))) {
            String line;
            while ((line = br.readLine()) != null) {
                records.add(line);
            }
        }

        return records;
    }
}*/
