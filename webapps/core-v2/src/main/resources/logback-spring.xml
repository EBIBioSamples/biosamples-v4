<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<!-- Import shared logback configuration from logback module -->
	<include resource="logback-shared.xml"/>
	
	<!-- Module-specific overrides can be added here if needed -->
</configuration>

